package com.programe.datastructure.tree.recursion;

/**
 * Node - an element is that contents some data and may have child nodes connected to it
 * Root Node - Topmost node, in the tree from which all the other nodes desand
 * Leaf Node - node having no children call leaf nodes
 * Depth - Level of node where it is resides (Root node level will be 0 )
 * Height - Length of longest path from Root node to Leaf Node
 * SubTree - that is part of the larger tree
 * Siblings - Nodes sharing the same parent
 * Ancestors - all nodes from parent to root upwards are ancestors of that node
 * Descendant - all nodes from child to leaf node are call descendant
 *
 */
public class TreeIntro {
}
