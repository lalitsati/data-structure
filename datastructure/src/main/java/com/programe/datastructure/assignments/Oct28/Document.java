package com.programe.datastructure.assignments.Oct28;

public class Document {
    /**
     * Memory Management -
     * Stack - insert and remove element from one entry point
     * also known as LIFO (Last In First Out)
     *
     * Call Stack - when we call method every method will store in stack first in first out from the stack
     *
     * Reference variable - variable storing address of the actual data & not storing the data
     *
     * Types of Memory -
     * 1. Stack - all preemitive data types and reference variables are stored in stack
     * 2. Heap - data pointed by the reference variables is stored in Heap example- Array,String
     *
     */
}
